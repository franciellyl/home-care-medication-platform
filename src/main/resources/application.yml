spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/homecare
    username: franlima
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
